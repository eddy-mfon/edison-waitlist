# Install devDependencies even in production environments.
# Required because `tsc --build` (typecheck) runs as part of the build step
# and needs devDependencies like @types/node, @tanstack/react-query, and zod
# to be present. Without this, Render skips devDeps and the build fails.
production=false
